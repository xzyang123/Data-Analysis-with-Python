# R-squared/ R^2

* R^2 close to 1 means the model prefectly fits the actual values

```python
X = df[['highway-mpg']]
Y = df['price']
lm.fit(X, Y)
lm.score(X, Y)
```








































































































